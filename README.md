# Christian Agyapong AI — Sales Consultant
RAG-powered retail chatbot for business sales in comfort.

## Setup
1. `pip install -r requirements.txt`
2. Configure `.env` with `GROQ_API_KEY`.
3. `python app.py`
